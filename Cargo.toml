[package]
name = "vcs-starship"
version = "0.0.1"
edition = "2024"
rust-version = "1.89"
description = "Git-first + JJ fallback Starship prompt module"
repository = "https://github.com/mikededo/vcs-starship"
license = "MIT"

[[bin]]
name = "vcs-starship"
path = "src/main.rs"

[features]
default = ["git"]
git = ["dep:git2"]

[dependencies]
# JJ integration
jj-lib = "0.37"

# Git integration
git2 = { version = "0.19", default-features = false, optional = true }

# CLI
clap = { version = "4.5", features = ["derive"] }

# Utilities
thiserror = "2.0"

[dev-dependencies]
tempfile = "3.24"

[profile.release]
lto = true
codegen-units = 1
strip = true
panic = "abort"

[lints.rust]
unsafe_code = "deny"

[lints.clippy]
all = { level = "warn", priority = -1 }
pedantic = { level = "warn", priority = -1 }
module_name_repetitions = "allow"
